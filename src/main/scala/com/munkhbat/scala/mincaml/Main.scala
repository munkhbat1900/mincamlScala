package com.munkhbat.scala.mincaml

import com.munkhbat.scala.mincaml.parser._
import org.antlr.v4.runtime.{ANTLRInputStream, CommonTokenStream}

import scala.io.Source

object Main {
  def main(args: Array[String]): Unit = {
//    val src = Source.fromFile("testFiles/sample.ml").mkString
//    val charStream = new ANTLRInputStream(src)
//    val lexer = new MincamlLexer(charStream)
//    val tokens = new CommonTokenStream(lexer)
//    val parser = new MincamlParser(tokens)
//
//    val visitor = new Visitor()
//    val ast = visitor.visit(parser.exp())
//
//    import sext._
//    println(ast.treeString)
  }
}
